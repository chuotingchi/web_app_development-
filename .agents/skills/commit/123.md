# Skill: Commit 

## Description
此技能會自動將目前的變更進行 Git Commit 並 Push 到 GitHub 遠端倉庫。

## Instructions
1. 檢查目前專案中所有已修改但尚未暫存的檔案。
2. 使用 `git add .` 將所有變更加入暫存區。
3. 根據變更內容撰寫簡短且具描述性的 commit message。
4. 執行 `git commit -m "你的訊息"`。
5. 執行 `git push` 將代碼推送到遠端。